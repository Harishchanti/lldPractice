package org.practice.ruleEngine;

public class DeletionService {
    public void process(User user) {
        // 1. Mark for deletion
       /* markSoftDelete(user);

        // 2. Publish event
        publishDeletionEvent(user);

        // 3. Async cleanup (PII removal)
        triggerDataPurge(user);*/
    }
}
